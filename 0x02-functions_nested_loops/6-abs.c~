#include "holberton.h"

/**
 * print_sign - check the code for Holberton School students.
 *
 *@r: is a parameter.
 *
 * Return: Always 0.
 */

int _abs(int r)
{
if (r > 0)
return (r);
else if (r < 0)
return (-r)
else
return (0);
}
